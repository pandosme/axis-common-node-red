version: '3'
services:
  nodered:
    container_name: axis-common-node-red
    image: nodered/node-red:latest
    restart: always
    environment:
      - TZ=Europe/Stockholm
    ports:
      - '8600:1880'
    volumes:
      - ./:/data
